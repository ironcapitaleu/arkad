name: Claude

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  pull_request:
    types: [opened]
  issues:
    types: [opened]

permissions:
  contents: write
  pull-requests: write
  issues: write
  id-token: write

jobs:
  claude:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude'))
    runs-on: ubuntu-latest
    env:
      AWS_BEARER_TOKEN_BEDROCK: ${{ secrets.CLAUDE_AWS_BEARER_TOKEN_BEDROCK }}
      AWS_REGION: ${{ vars.CLAUDE_AWS_REGION }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: anthropics/claude-code-action@v1
        with:
          use_bedrock: "true"
          github_token: ${{ secrets.GITHUB_TOKEN }}
          claude_args: '--model us.anthropic.claude-sonnet-4-6'

  claude-auto-review:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    env:
      AWS_BEARER_TOKEN_BEDROCK: ${{ secrets.CLAUDE_AWS_BEARER_TOKEN_BEDROCK }}
      AWS_REGION: ${{ vars.CLAUDE_AWS_REGION }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: anthropics/claude-code-action@v1
        with:
          use_bedrock: "true"
          github_token: ${{ secrets.GITHUB_TOKEN }}
          claude_args: '--model us.anthropic.claude-sonnet-4-6'
          prompt: |
            Review this PR using the project's AGENTS.md "PR Review Guidelines" section.

            1. Post a top-level comment with a 2-3 bullet point summary of the changes.

            2. For each finding, post an inline review comment on the specific file and line.
               Use GitHub's suggestion syntax when you can provide a concrete fix:

               ```suggestion
               corrected code here
               ```

               This gives the author a one-click "Commit suggestion" button.

            3. Categorize findings by severity:
               - 🔴 Bug / security issue / correctness problem (must fix)
               - 🟡 Improvement / performance / style (should fix)
               - 🟣 Nit / minor (optional)

            4. If the PR looks clean, just post the summary and approve.
